name: Deploy frontend to Azure Storage

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: frontend-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy static resume
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://${{ vars.PUBLIC_HOSTNAME }}
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate deployment configuration
        shell: bash
        env:
          AZURE_STORAGE_ACCOUNT_NAME: ${{ vars.AZURE_STORAGE_ACCOUNT_NAME }}
          PUBLIC_HOSTNAME: ${{ vars.PUBLIC_HOSTNAME }}
        run: |
          set -euo pipefail

          if [[ -z "${AZURE_STORAGE_ACCOUNT_NAME}" ]]; then
            echo "::error::Missing production environment variable: AZURE_STORAGE_ACCOUNT_NAME"
            exit 1
          fi

          if [[ -z "${PUBLIC_HOSTNAME}" ]]; then
            echo "::error::Missing production environment variable: PUBLIC_HOSTNAME"
            exit 1
          fi

          if [[ ! "${PUBLIC_HOSTNAME}" =~ ^[A-Za-z0-9.-]+$ ]]; then
            echo "::error::PUBLIC_HOSTNAME is not a valid hostname"
            exit 1
          fi

          if [[ ! -f site/index.html ]]; then
            echo "::error::site/index.html is required before frontend deployment."
            exit 1
          fi

          if find site -type f \( -name '*.pem' -o -name '*.key' -o -name '.env' -o -name '.env.*' \) -print -quit | grep -q .; then
            echo "::error::Potential credential-bearing file found in site/."
            exit 1
          fi

          if grep -RniE 'AccountKey=|DefaultEndpointsProtocol=|SharedAccessSignature=|client_secret|AZURE_CLIENT_SECRET|COSMOS_CONNECTION_STRING' site; then
            echo "::error::Potential Azure/database credential material found in frontend artifacts."
            exit 1
          fi

      - name: Install frontend dependencies
        shell: bash
        run: |
          set -euo pipefail

          if [[ -f package-lock.json ]]; then
            if [[ ! -f package.json ]]; then
              echo "::error::package-lock.json exists but package.json is missing."
              exit 1
            fi
            npm ci
          elif [[ -f package.json ]]; then
            npm install
          else
            echo "No frontend package manifest is present; approved frontend architecture is plain HTML/CSS/JavaScript with no dependency manifest."
          fi

      - name: Build production static website
        shell: bash
        run: |
          set -euo pipefail

          rm -rf dist
          mkdir -p dist
          cp -a site/. dist/

          test -f dist/index.html
          test "$(find dist -type f | wc -l)" -gt 0

          mkdir -p deployment-evidence
          sha256sum $(find dist -type f -print | sort) > deployment-evidence/dist-sha256.txt
          find dist -type f -printf '%P\n' | sort > deployment-evidence/dist-manifest.txt

      - name: Upload deployment evidence
        uses: actions/upload-artifact@v4
        with:
          name: frontend-deployment-evidence-${{ github.sha }}
          path: deployment-evidence/
          retention-days: 14

      - name: Azure login with OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: false

      - name: Upload static website using Microsoft Entra authorization
        id: storage_upload
        shell: bash
        env:
          AZURE_CORE_OUTPUT: none
          AZURE_STORAGE_ACCOUNT_NAME: ${{ vars.AZURE_STORAGE_ACCOUNT_NAME }}
        run: |
          set -euo pipefail

          az storage blob upload-batch \
            --account-name "${AZURE_STORAGE_ACCOUNT_NAME}" \
            --destination '$web' \
            --source dist \
            --auth-mode login \
            --overwrite

      - name: Verify deployed HTTPS endpoint
        id: https_verify
        shell: bash
        env:
          PUBLIC_HOSTNAME: ${{ vars.PUBLIC_HOSTNAME }}
        run: |
          set -euo pipefail

          curl --fail --silent --show-error --location \
            --proto '=https' \
            --proto-redir '=https' \
            --max-time 20 \
            "https://${PUBLIC_HOSTNAME}/" \
            > /tmp/resume.html

          grep -qi '<html' /tmp/resume.html

      - name: Record deployment result
        if: always()
        shell: bash
        env:
          UPLOAD_OUTCOME: ${{ steps.storage_upload.outcome }}
          HTTPS_OUTCOME: ${{ steps.https_verify.outcome }}
          PUBLIC_HOSTNAME: ${{ vars.PUBLIC_HOSTNAME }}
        run: |
          set -euo pipefail
          mkdir -p deployment-evidence
          {
            echo "commit_sha=${GITHUB_SHA}"
            echo "workflow_run=${GITHUB_RUN_ID}"
            echo "upload_outcome=${UPLOAD_OUTCOME}"
            echo "https_verification_outcome=${HTTPS_OUTCOME}"
            echo "public_hostname=${PUBLIC_HOSTNAME}"
          } > deployment-evidence/deployment-result.txt

      - name: Upload final deployment evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontend-final-deployment-evidence-${{ github.sha }}
          path: deployment-evidence/
          retention-days: 14

      - name: Azure logout
        if: always()
        shell: bash
        run: az logout
